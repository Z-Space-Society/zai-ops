"""ATProto OAuth client routes.

Populated in Task 2 (client-metadata.json) and Task 3 (login + callback).
Empty for now so the project boots with the app installed.
"""

from django.urls import path

app_name = "atproto_oauth"

urlpatterns: list[path] = []
